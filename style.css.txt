body {
    margin: 0;
    height: 100vh;
    font-family: 'Baloo 2', cursive;

    /* YOUR BACKGROUND IMAGE */
    background: url("images/background.jpg") no-repeat center center;
    background-size: cover;

    display: flex;
    justify-content: center;
    align-items: center;
}

.container {
    text-align: center;
}

/* Title box (center white area style) */
.title-box {
    background: rgba(255,255,255,0.9);
    padding: 30px;
    border-radius: 20px;
    width: 70%;
    margin: auto;
}

.welcome {
    background: #f4c542;
    padding: 5px 15px;
    border-radius: 15px;
    font-size: 14px;
}

h1 {
    margin: 10px 0;
    font-size: 36px;
    color: #333;
}

p {
    color: #555;
}

/* Buttons */
.menu {
    margin-top: 20px;
}

.menu button {
    background: #f4c542;
    border: 2px solid #333;
    border-radius: 10px;
    padding: 6px 14px;
    margin: 5px;
    cursor: pointer;
    font-weight: bold;
}

.menu button:hover {
    background: #ffd966;
}

/* Content */
.content-box {
    margin-top: 20px;
    font-size: 20px;
    color: #333;
}